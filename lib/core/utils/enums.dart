/// Enum to represent different subscription types
enum SubscriptionType {
  monthly,
  yearly,
  none,
}

/// Enum to represent app flow states
enum AppFlowState {
  splash,
  onboarding,
  paywall,
  mainApp,
}
